; PlatformIO Project Configuration
; T-Lora-Pager-SkinnyCon (LilyGoLib)
;
; Native environments for CI testing, ESP32-S3 env for compilation check.

[platformio]
default_envs = native, native_lvgl

; ============================================================
; Native unit tests (Unity) — no board, no framework
; Tests hardware logic, data structures, parsing
; ============================================================
[env:native]
platform = native
test_framework = unity
build_flags =
    -std=c++17
    -DUNITY_INCLUDE_DOUBLE
    -DLVGL_SIMULATOR
    -DLV_CONF_INCLUDE_SIMPLE
    -Itest/mocks
    -Itest/simulator
test_filter = test_hardware*

; ============================================================
; Native LVGL headless simulator tests (Unity + LVGL 9.2)
; Renders to framebuffer, exports PPM screenshots
; ============================================================
[env:native_lvgl]
platform = native
test_framework = unity
build_flags =
    -std=c++17
    -DUNITY_INCLUDE_DOUBLE
    -DLVGL_SIMULATOR
    -DLV_CONF_INCLUDE_SIMPLE
    -DLV_USE_STDLIB_MALLOC=LV_STDLIB_CLIB
    -Itest/mocks
    -Itest/simulator
lib_deps =
    lvgl/lvgl@^9.2.0
test_filter = test_lvgl_*

; ============================================================
; ESP32-S3 compilation check (no upload)
; Validates that library compiles against real SDK
; ============================================================
[env:tlora_pager]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino
build_flags =
    -DARDUINO_T_LORA_PAGER
    -DARDUINO_LILYGO_LORA_SX1262
    -DUSING_INPUT_DEV_KEYBOARD
    -DLV_CONF_INCLUDE_SIMPLE
    -DBOARD_HAS_PSRAM
    -Isrc
lib_deps =
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.0.0
    mikalhart/TinyGPSPlus@^1.1.0
    adafruit/Adafruit TCA8418@^1.0.0
    jgromes/RadioLib@^7.0.0
