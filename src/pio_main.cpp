/* PlatformIO build entry — .ino files aren't auto-processed outside src_dir.
 * Lives in src/ so Arduino IDE (which compiles all .cpp in the sketch dir)
 * never sees it, avoiding duplicate setup()/loop() definitions. */
#include "Arduino.h"
#include "factory.ino"
